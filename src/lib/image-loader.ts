// src/lib/image-loader.ts
import imageData from './placeholder-images.json';

// This is the correct way to access the array within the imported JSON
export const placeholderImages = imageData.placeholderImages;
